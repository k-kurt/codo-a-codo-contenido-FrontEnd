creamos un archivo app.js(si no te lo pide luego)
nos ubicamos en el directorio en la consola
instalamos express con npm i express
indicamos en el package.json lo de start junto con lo de nodemund para actulizar los cambios
MULTER es un middleword que vamos a utilizar para gestionar archivos
lo instalamos con npm i multer
hay que inicializar el proyecto, sino no podemos agregar el npm start en el package.json, lo inicializamos con el npm init -y
agregamos el "scripts" lo siguiente  "start": "node --watch app.js"